// Logical conversion from JSON to HTML & Vice-Versa

/**
 * A simple function  to initiate an intance of the editor (ToDo: Check for multi-instance)
 * @returns void
 */
export const init = (params: string): void => {
    console.log(params)
    // if (params && params?.id)
}
